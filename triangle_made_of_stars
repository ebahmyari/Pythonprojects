# call sleep function to see whats going on in the loop
from time import sleep

# number of rows which triangle is made of
n = int(input("enter an integer:"))

# loop start iterator
i =1
while i<=n:
    print(("* " * (i)).center(3*n))
    sleep(1)
    i = i+1  
